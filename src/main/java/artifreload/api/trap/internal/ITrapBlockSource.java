package artifreload.api.trap.internal;

import net.minecraft.block.state.IBlockState;
import net.minecraft.dispenser.IBlockSource;
import net.minecraft.tileentity.TileEntity;
import net.minecraft.util.math.BlockPos;


public interface ITrapBlockSource extends IBlockSource {

/**
 * This is just an example of IBlockSource you could add stuff to if the need ever arises
 * "Probably never going to use this but just in case"
 */

	/*
	double getX();

	double getY();

	double getZ();

	BlockPos getBlockPos();

	IBlockState getBlockState();

	<T extends TileEntity> T getBlockTileEntity();
*/
}
